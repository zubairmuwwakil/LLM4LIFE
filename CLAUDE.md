# Claude Instructions

Read [`AGENTS.md`](AGENTS.md) first.

This file intentionally does not duplicate architecture rules. Canonical agent guidance lives in `AGENTS.md`, and machine-readable current state lives in `system.yaml`.

When changing the architecture, update the canonical files and append the rationale to `docs/DECISIONS.md`.
